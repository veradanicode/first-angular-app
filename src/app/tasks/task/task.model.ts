export interface TaskComponent {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}

export interface newTask {
  title: string;
  summary: string;
  dueDate: string;
}
